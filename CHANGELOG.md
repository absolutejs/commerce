# Changelog

All notable changes to `@absolutejs/commerce`.

This file is generated by `absolute-changelog` from the entries in
`changelog/`. Edit an entry, not this file — and add new ones under
`changelog/unreleased/`.

## 0.40.1-beta.55 — 2026-09-20

### Added

- **Refresh supplier inventory independently of catalog content with product-level lookups, bounded concurrency, partial failure reporting, shared source leases and preserved observation timestamps** (`refreshSupplierInventory`, `refreshCatalogInventory`, `inventoryObservation`, `preserveSupplierStock`)

### Changed

- **Add optional product inventory lookup and observation transport to provider contracts** (`InventoryLevel`, `CatalogSourceProvider`)
- **Regenerate inferred return declarations with reordered object members; runtime and structural types are unchanged** (`createReview`, `createInvoice`, `createGiftPool`, `upsertMembership`, `createReturnRequest`, `createCatalog`, `upsertCatalogProduct`, `createCatalogListing`, `createCatalogCollection`, `offerCatalogProduct`, `createStorefrontMerchandisingService`, `createStorefrontPaymentService`, `createStorefrontAftercareEvidenceService`, `emitStorefrontOrderEvent`, `createStorefrontOrderService`, `emitStorefrontCaseEvent`, `createStorefrontAftercareService`, `recordStorefrontDispute`)

### Fixed

- **Ship supplier stock evidence evaluation and preserve stock observations during catalog refreshes** (`evaluateSupplierStock`, `synchronizeCatalogSource`)

## 0.40.1-beta.54 — 2026-09-14

### Fixed

- **Match displayed order references containing punctuation while retaining the buyer email requirement** (`findOrderForTracking`)

## 0.40.1-beta.53 — 2026-09-11

### Fixed

- **Apply shared country address requirements to fulfillment without fabricated region or postal values, retaining required US state and ZIP validation** (`validateFulfillmentOrder`, `addressRequiredFields`, `supportedAddressCountries`)

## 0.40.1-beta.52 — 2026-09-10

### Fixed

- **Refresh legacy variants whose source metadata is missing, preserving product-scoped variant identities** (`synchronizeCatalogSource`)

## 0.40.1-beta.51 — 2026-09-10

### Fixed

- **Catalog refresh preserves existing product and variant identities and batches variant writes atomically** (`synchronizeCatalogSource`)
